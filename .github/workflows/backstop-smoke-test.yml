name: Backstop Smoke Tests

on:
  workflow_dispatch:
  workflow_call:

permissions:
  actions: write
  contents: write
  pull-requests: write

env:
  NODE_VERSION: 20

jobs:
  smoke-puppeteer:
    name: 💨 Puppeteer
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: ${{ github.event.pull_request.head.sha || github.ref }}

      - name: ⬢ Setup Node & Cache
        uses: actions/setup-node@v4
        with:
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: ↧ Install
        run: npm ci

      - name: "𓋏 Run `npm run smoke-test`"
        continue-on-error: true
        run: |
          npm run smoke-test

      - name: "Validate Puppeteer Test Results"
        id: validate
        continue-on-error: true
        run: |
          set +e
          RESULT=$(diff -c <(jq -S 'walk(if type == "object" then with_entries(.value |= if type == "object" or type == "array" then . else "" end) else . end) | del(.tests[].pair.diff)' test/__fixtures__/smoke-test.json) \
            <(jq -S 'walk(if type == "object" then with_entries(.value |= if type == "object" or type == "array" then . else "" end) else . end) | del(.tests[].pair.diff)' test/configs/backstop_data/bitmaps_test/**/report.json))
          if [[ $RESULT == "0" ]]; then
            echo "# ✅ Puppeteer Test Results Validated" >> $GITHUB_STEP_SUMMARY
          else
            echo "# ❎ Puppeteer Test Results Invalid — Differences:" >> $GITHUB_STEP_SUMMARY
            echo '```diff' >> $GITHUB_STEP_SUMMARY
            echo $(diff -c <(jq -S . test/__fixtures__/smoke-test.json) \
            <(jq -S . test/configs/backstop_data/bitmaps_test/**/report.json)) >> $GITHUB_STEP_SUMMARY
            echo '```'
          fi

      - name: "Test Results Validation Outcome"
        run: |
          if [[ $RESULT == "0" ]]; then
            exit 0
          else
            exit 1
          fi

  smoke-playwright:
    name: 💨 Playwright
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: ${{ github.event.pull_request.head.sha || github.ref }}

      - name: ⬢ Setup Node & Cache
        uses: actions/setup-node@v4
        with:
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: ↧ Install
        run: npm ci

      - name: "🎭 Run `npm run smoke-test-playwright`"
        continue-on-error: true
        run: |
          npx playwright install --with-deps
          npm run smoke-test-playwright

      - name: "Validate Playwright Test Results"
        id: validate
        continue-on-error: true
        run: |
          set +e
          RESULT=$(diff -c <(jq -S 'walk(if type == "object" then with_entries(.value |= if type == "object" or type == "array" then . else "" end) else . end) | del(.tests[].pair.diff)' test/__fixtures__/smoke-test-playwright.json) \
            <(jq -S 'walk(if type == "object" then with_entries(.value |= if type == "object" or type == "array" then . else "" end) else . end) | del(.tests[].pair.diff)' test/configs/backstop_data/bitmaps_test/**/report.json))
          if [[ $RESULT == "0" ]]; then
            echo "# ✅ Playwright Test Results Validated" >> $GITHUB_STEP_SUMMARY
          else
            echo "# ❎ Playwright Test Results Invalid — Differences:" >> $GITHUB_STEP_SUMMARY
            echo '```diff' >> $GITHUB_STEP_SUMMARY
            echo $(diff -c <(jq -S . test/__fixtures__/smoke-test-playwright.json) \
            <(jq -S . test/configs/backstop_data/bitmaps_test/**/report.json)) >> $GITHUB_STEP_SUMMARY
            echo '```'
          fi

      - name: "Test Results Validation Outcome"
        run: |
          if [[ $RESULT == "0" ]]; then
            exit 0
          else
            exit 1
          fi
